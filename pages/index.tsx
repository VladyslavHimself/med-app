import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
    return (
        <div>
            <Head>
                <title>VITech Med App</title>
            </Head>
        </div>
    );
};

export default Home;
